[project]
name = "openenv-email-triage"
version = "0.1.0"
description = "OpenEnv Email Triage"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv>=0.2.0"
]

[project.scripts]
server = "openenv_email_triage.server:app"

[tool.setuptools]
packages = ["openenv_email_triage"]
